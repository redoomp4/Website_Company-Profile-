# Deployment

1. Jalankan `npm ci` untuk memasang dependensi sesuai lockfile.
2. Jalankan `npm run build`.
3. Unggah isi folder `dist` ke layanan hosting statis.

Jangan mengubah isi `dist` secara manual.
